/* use rug::Integer;

use crate::{point::Point, s256_field::S256Field};

#[derive(PartialEq, Eq, Debug, Clone)]
pub struct S256Point {
    point: Point<S256Field>,
}

impl S256Point {
    pub fn new(x: S256Field, y: S256Field) -> Self {
        let a = S256Field::new(Integer::from(0));
        let b = S256Field::new(Integer::from(7));

        Self { x, y }
    }
} */
